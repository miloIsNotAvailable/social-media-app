import { FC } from "react";
import Form from "../../custom/Form";

const Password: FC = () => {

    return (
        <Form
            type={ "password" }
            placeholder={ "password" }
        />
    )
}

export default Password