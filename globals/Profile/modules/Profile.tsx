import { FC } from "react";
import { styles } from "../styles";

const Profile: FC = () => {

    return (
        <div className={ styles.profile }>
            
        </div>
    )
}

export default Profile