import { FC } from 'react'
import { styles } from "../styles"

const SendComment: FC = () => {

    return <button className={ styles.send_comment }>
        send
    </button>
}

export default SendComment